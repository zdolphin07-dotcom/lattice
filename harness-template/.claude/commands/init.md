Read `lattice/skills/init.md` and follow its flow to initialize Lattice.

Ask the user for project info (language, framework, ORM, database), generate `lattice/manifest.yaml`, inject the `CLAUDE.md` `@import`, create directory structure, then run bootstrap to verify.
