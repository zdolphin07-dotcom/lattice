---
description: Legacy alias for /verify; branch closeout is optional outside the main PrismSpec workflow
---

Use `prismspec/commands/verify.md`.

If durable lessons should be promoted after verification, use `prismspec/commands/capture.md`.
